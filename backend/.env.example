# 复制本文件为 .env 并填入真实值。.env 已被 git 忽略，不会上传。

# DeepSeek API Key（在 https://platform.deepseek.com 申请）
# 不填则后端进入"演示模式"：只返回检索到的资料，不调用 LLM
DEEPSEEK_API_KEY=

# 一般不用改
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_MODEL=deepseek-v4-pro
